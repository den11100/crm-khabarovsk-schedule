# 📘 База khb список таблиц:
## wp_erp_analysis

| Поле               | Тип данных                  | Назначение                                                                 |
|--------------------|-----------------------------|----------------------------------------------------------------------------|
| `ID`               | bigint(20) unsigned         | Уникальный идентификатор записи, автоинкрементное поле (первичный ключ).  |
| `erp_analysis_id`  | int(11)                     | Внешний ключ, ссылается на анализ в ERP-системе.                          |
| `lid`              | int(11) unsigned            | Код                                                                      |
| `name`             | varchar(255)                | Название услуги/теста                                                      |
| `price`            | decimal(10,2)               | Цена услуги/теста                                                        |
| `is_deleted`       | tinyint(1)                  | Флаг удаления: 0 — активная запись, 1 — удалена (мягкое удаление).       |
| `analysis_group_id`| int(11) unsigned            | ERP Идентификатор группы, к которой относится анализ.                         |

## wp_crm_room
| Поле                | Тип данных         | Назначение                                                                                       |
|---------------------|--------------------|--------------------------------------------------------------------------------------------------|
| `ID`                | bigint(20) unsigned| Уникальный идентификатор записи (первичный ключ).                                                |
| `crm_room_id`       | int(11)            | ID врача или кабинета в CRM-системе.                                                             |
| `erp_employee_doctor`| int(11)           | ID врача в ERP-системе. Для процедурных кабинетов — `0`.                                         |
| `title`             | text               | Заголовок (ФИО, Должность, Специализация, Регалии)                             |
| `fio`               | varchar(255)       | ФИО врача или название кабинета.                                                                 |
| `achievements`      | text               | Регалии или достижения врача.                                                                    |
| `experience`        | varchar(255)       | Стаж работы.                                                                                      |
| `education`         | text               | Образование врача.                                                                               |
| `specialty_name`    | varchar(255)       | Название специальности.                                                                          |
| `is_deleted`        | tinyint(1)         | Флаг мягкого удаления: `0` — активна, `1` — удалена.                                              |
| `is_active_api`     | tinyint(1)         | Флаг активности для API: `0` — слоты не выдаются, `1` — слоты доступны.                          |
| `erp_analysis_ids`  | text               | список erp_analysis_id через запятую  (услуги закрепленные за врачём)                            |







# 📘 API: Для внешнего сайта Хабаровск
Сгенерируй документацию API в формате Markdown, в одном цельном блоке без разбивки по частям.

## 📦 Особенности обработки ошибок

Все ответы от API приходят с HTTP статусом **200 OK**, вне зависимости от результата выполнения запроса.  
Информацию об успешности операции необходимо проверять по полю `status` в теле ответа:

- `"status": "success"` — операция выполнена успешно.
- `"status": "fail"` — произошла ошибка, описание находится в полях `error` и `code`.
> ⚠️ В некоторых случаях при нарушении протокола (например, неправильный HTTP метод) может быть возвращён 400 Bad Request или другие стандартные HTTP ошибки.


## 🔐 Аутентификация

Все методы требуют заголовок:

X-Api-Key: {your_api_key}

При отсутствии или неправильном API-ключе:
```json
{
  "status": "fail",
  "error": "Не верный API ключ",
  "code": 2
}
```


## 📘 Получение ячеек Врачей(кабинетов)

### 📡 Метод запроса
`POST /api/khabarovsk/schedule/cells`

---

### 📝 Описание
Метод возвращает список ячеек (свободных и занятых) на ближайшие 14 дней, начиная с текущей даты.<br>
Если не возвращается какой-то врач/кабинет, значит у него не активировано API или не установленно раписание
При необходимости можно ограничить выборку конкретными врачами/кабинетами с помощью параметра `crm_room_ids`.

---

### 🔧 Тело запроса (JSON)
`crm_room_ids` - Массив ID кабинетов. Если не указан, возвращаются все врачи/кабинеты по которым есть расписание.

#### 📥 Пример запроса

```json
{
  "crm_room_ids": [3, 5, 7]
}
```

#### Ответ
```json
{
  "status": "success",
  "data": {
    "1": {
      "3": {
        "fio": "Иванов Иван Иванович",
        "crm_room_id": 3,
        "erp_employee_doctor_id": 22,
        "cells": [
          {
            "dt": "2025-05-21",
            "time_start": "08:00:00",
            "time_end": "08:10:00",
            "free": 1,
            "type": 1,
            "crm_appointment_id": null,
          },
          {
            "dt": "2025-05-21",
            "time_start": "08:10:00",
            "time_end": "08:20:00",
            "free": 0,
            "type": 1,
            "crm_appointment_id": 102001,
          }
        ]
      }
    }
  }
}
```

 "1" - это crm.office.id<br>
 "3" - это crm.room.id<br>
 
Придут все ячейки (свободные и занятые)<br>
`free = 1` - свободна<br>
`free = 0` - занята<br>

`type = 1` - офлайн приём.<br>
`type = 3` - онлайн приём.<br>

`crm_room_id` будет всегда<br>
`erp_employee_doctor_id` > 0 будет у врачей<br>


#### Ответ 200 ОК, если приходит пустой массив значит не активно api у кабинета или он помечен как удаленный или не задано рассписание.
```json
{
    "status": "success",
    "data": []
}
```

#### Ответ 200 ОК
Если какие либо ошибки
```json
{
  "status": "fail",
  "error": "Текст ошибки",
  "code": (int) код ошибки
}
```


# Бронирование слота

### POST `/api/khabarovsk/schedule/reserve-slot`

Бронирование слота на 20 минут.

> ⚠️ Неподтвержденные слоты автоматически удаляются через 20 минут

### Описание

Позволяет забронировать слот.<br>
В случаи успешного бронирования возвращает id записи crm_appointment_id - он понадобится при подтверждении.

### Обязательные параметры (JSON тело запроса)
| Поле                    | Тип     | Описание                                              |
|-------------------------|---------|--------------------------------------------------------|
| crm_room_id             | int     | Уникальный идентификатор врача/кабинета               |
| analysis_id            | int     | ID теста (из `erp.analysis.id`) у каждого теста есть время выполнения. time_end  высчитываю сам |
| dt                     | string  | Дата процедуры (в формате `Y-m-d`)                    |
| time_start             | string  | Время начала процедуры (в формате `H:i:s`)            |



### Пример запроса 1
```json
{
  "crm_room_id": 3,
  "analysis_id": 25,
  "dt": "2025-05-21",
  "time_start": "08:00:00",
}
```

#### Ответ 200 ОК
```json
{
    "status": "success",
    "data": {
        "crm_appointment_id": 98435
    }
}
```


```json
{
    "status": "fail",
    "error": "Данное время уже занято.",
    "code": 13
}
```



# Подтверждение слота.
### URL: `/api/khabarovsk/schedule/confirm-appointment`


#### Обязательные параметры:

| Ключ                | Тип     | Описание                                           |
|---------------------|---------|----------------------------------------------------|
| `crm_appointment_id`| int     | crm.appointment.id  - выдан при резервировании слота                  |
| `p_email`           | string  | Email пациента                                     |
| `p_family`          | string  | Фамилия пациента                                   |
| `p_name`            | string  | Имя пациента                                       |
| `p_sex`             | int     | Пол пациента (1 - М; 2 - Ж)                        |
| `p_phone`           | string  | (только цифры) для российских номеров начинается с 7 (ни икаких 8)                     |
| `p_birth`           | string  | Дата рождения в формате `Y-m-d`                    |


#### Необязательные параметры:

| Ключ                | Тип     | Описание                                           |
|---------------------|---------|----------------------------------------------------|
| `p_patronymic`      | string  | Отчество пациента                                  |

---

---
### Пример запроса
```json
{
  "crm_appointment_id": 98435,
  "p_email": "patient@example.com",
  "p_family": "Иванов",
  "p_name": "Иван", 
  "p_patronymic": "Иванович",
  "p_sex": 1,
  "p_phone": "79001234567",
  "p_birth": "1980-12-30",
}
```

#### Ответ 200 ОК
```json
{
    "status": "success",
    "data": [
        "Бронирование подтверждено"
    ]
}
```


#### Ошибки 200 ОК 
```json
{
    "status": "fail",
    "error": "Не переданы обязательные параметры: crm_appointment_id",
    "code": 10
}
```

```json
{
    "status": "fail",
    "error": "Бронирование слота не найдено",
    "code": 14
}
```

```json
{
    "status": "fail",
    "error": "Ошибка создания пациента: Значение «Email заказчика» не является правильным email адресом.",
    "code": 14
}
```

# Отмена бронирования

### POST `/api/khabarovsk/schedule/cancel-appointment`

### Обязательные параметры (JSON тело запроса)
| Поле                    | Тип     | Описание                                              |
|-------------------------|---------|--------------------------------------------------------|
| `crm_appointment_id`    | int     | crm.appointment.id  - выдан при резервировании слота       |


### Пример запроса
```json
{
  "crm_appointment_id": 144555,  
}
```

#### Ответ 200 ОК
```json
{
    "status": "success",
    "data": ["Запись успешно удалена"]
}
```

### Ошибки Ответ 200 ОК
```json
{
    "status": "fail",
    "error": "Не найдена запись с ID 144555",
    "code": 8
}
```




## 🧾 Коды ошибок API

Описание всех возможных кодов ошибок, возвращаемых в поле `code` при статусе `"fail"`:

| Код | Константа                     | Описание                                                         |
|-----|-------------------------------|------------------------------------------------------------------|
| 2   | `CODE_BAD_API`                | Неверный API-ключ. Клиент не прошёл проверку авторизации.        |
| 8   | `CODE_NOT_FOUND`              | Запрашиваемый объект  не найден.                                 |
| 9   | `CODE_INVALID_FORMAT`         | Ошибка формата данных — например, тело запроса невалидный JSON.  |
| 10  | `CODE_REQUIRED_PARAM`         | Не переданы обязательные параметры запроса.                      |
| 11  | `CODE_ALREADY_EXIST`          | Объект с таким значением уже существует .                        |
| 12  | `CODE_VALIDATION`             | Ошибка валидации модели — некорректные или пустые значения.      |
| 13  | `CODE_VALIDATION_OR_NO_VACANT`| Ошибка валидации модели или слот занят                           |
| 14  | `CODE_ERROR_CONFIRM`          | Ошибка подтверждения слота.                                      |


